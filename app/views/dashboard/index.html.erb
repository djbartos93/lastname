<p id="notice"><%= notice %></p>

<div class="container">
<div class="row">
<div class="col">
  <h1> Latest Upload </h1>
  <h2><%= @videos.name %></h2>
  <br>
  Streamed on <%= @videos.recording_date %> - <td><%= link_to 'More Info', @videos %></td>
  </div>
</div>

<div class="row">
  <div class="col">
    <video controls>
      <source src="<%= url_for(@videos.stream_recording_video) %>">
    </video>
  </div>
</div>
</div>
